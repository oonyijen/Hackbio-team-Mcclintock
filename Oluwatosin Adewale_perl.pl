#!/usr/bin/perl -w
sub ellipse_area {
  my ($r1, $r2) = @_;
   $sum=0
   for (i=0,i<$r1.length,i++)
            if($r1[i]!=$r2[i])
                    $sum = $sum+1
  return $sum;
  }
$name = "Oluwatosin Adewale";
$email = "oluwatosinadewale@gmail.com";
$slack_username = "@Halton";
$biostack = "Drug development";
$twitter_username = "@realDrtee";
$hamming_distance=$sum
print "$name, ", "$email, ", "$slack_username, ", "$biostack, ", "$twitter_username ", "$hamming_distance";